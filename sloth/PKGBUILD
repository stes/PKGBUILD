#Automatically generated by pip2arch on 2017-02-03

pkgname=python-sloth-git
pkgver=r589.66d3518
pkgrel=1
pkgdesc="sloth is a tool for labeling image and video data for computer vision research."
url="http://sloth.readthedocs.org/"
depends=('python' )
makedepends=('python3' )
license=('CUSTOM')
arch=('any')
source=('git+https://github.com/cvhciKIT/sloth.git')
sha256sums=('SKIP')

_pkgname=git/sloth

package() {
    makepkg -p "$_pkgname"
    cd "$_pkgname"
    python setup.py install --root="$pkgdir" --optimize=1 
}



pkgver() {
    cd "$_pkgname"
    (   
        set -o pipefail
        git describe --long 2>/dev/null | sed 's/\([^-]*-g\)/r\1/;s/-/./g' ||
        printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
    )
}

